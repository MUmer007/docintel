# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Data & model artifacts (never commit raw docs, vector store, or large eval outputs)
data/raw/*
data/processed/*
data/chroma/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/chroma/.gitkeep
*.log

# IDE / OS
.vscode/
.idea/
.DS_Store

# Test / coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Notebooks checkpoints
.ipynb_checkpoints/

# Eval run outputs are generated artifacts (timestamped, accumulate over time) --
# but the gold dataset itself (gold_qa.json) IS tracked, since it's real project input.
data/eval_datasets/results_*.json
